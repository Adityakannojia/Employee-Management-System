# TODO

- [x] Fix runtime bug in `changeUserProfile` (undefined `incomingRefreshToken`).
- [x] Fix `getCurrentEmployee` success response incorrectly using `ApiError` instead of `ApiResponse`.
- [x] Fix `createWorkLog` invalid `.populate()` usage.

- [x] Re-check for any other obvious runtime/logic errors in controllers.
- [ ] Run a quick start/check command (npm test/start or node import) to ensure code loads.

- [ ] Add comments across the codebase where helpful (controllers, middleware, utils).
